{% layout "none" %}
<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="utf-8">
    <title>{{mixture.project_name}} - Übersicht aller statischen HTML-Dateien</title>
    <style type="text/css">
    <!--
        html {
            height: 100%;
        }
        body {
            padding: 0;
            margin: 0;
            overflow: hidden;
            height: 100%;
            font-family: Arial, Helvetica, sans-serif;
            font-size: 12px;
        }
        main {
            float: left;
            width: 75%;
            height: 100%;
        }
        main iframe {
            width: 100%;
            height: 100%;
            border: none;
        }
        aside {
            float: right;
            width: 25%;
            height: 100%;
            border-left: 1px solid white;
            background-color: #d6dff2;
            overflow-x: hidden;
            overflow-y: scroll;
            -webkit-box-sizing: border-box;
            -moz-box-sizing: border-box;
            box-sizing: border-box;
        }
        aside ul {
            padding: 0;
            margin: 10px 0 0 20px;
        }
        aside ul li {
            color: #000065;
        }
        aside ul li:first-child {
            display: none; /*quickfix*/
        }
        aside ul li:hover {
            color: #000000;
        }
        aside ul li a {
            text-decoration: none;
            color: #000065;
            padding: 3px 5px;
            display: inline-block;
        }
        aside ul li a:hover {
            color: #000000;
            background-color: white;
        }
    -->
    </style>
</head>
<body>

<main>
    <iframe src="homepage" name="frame" id="frame">
        <p>Ihr Browser kann leider keine eingebetteten Frames anzeigen:<br>
        Sie können die eingebettete Seite Über den folgenden Verweis aufrufen: <a href="homepage">Homepage</a></p>
    </iframe>
</main>
<aside id="aside">
    {{ mixture.sitemap | navigation: mixture.url, 'ul', 'li', 'selected', 'Page overview' }}
</aside>

<script>
    var myIframe    = document.getElementById('frame'),
        a           = document.getElementById('aside')
                    .getElementsByTagName('a');

    for(var i in a) {
        a[i].addEventListener('mouseenter', function(event) {
            myIframe.src = event.target.href;
        });
    }
</script>

</body>
</html>